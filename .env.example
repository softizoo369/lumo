# Copy to .env in development and fill values there (do NOT commit .env to VCS)
SECRET_KEY=replace-me-with-a-secure-secret
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
# Example: sqlite (default) or postgres URL: postgres://user:pass@host:5432/dbname
DATABASE_URL=sqlite:////home/nahid/Desktop/lumo/backend/db.sqlite3
